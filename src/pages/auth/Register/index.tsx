import { Button } from "@/components/ui/button";
import React from "react";

export default function Register() {
  return (
    <div >
      <Button onClick={() => console.log("fasd")}>Click me</Button>
    </div>
  );
}
